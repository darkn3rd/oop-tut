# -*- mode: ruby -*-
# vi: set ft=ruby :

PACKAGE_DIR="/vagrant/.packages"  # Store Packages Here

Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/trusty64"
  config.vm.hostname = "oop-tutorial-ubuntu"

  # config.vm.provision "shell", inline: <<-SHELL
  #   apt-get update
  #   apt-get install -y apache2
  # SHELL
end
